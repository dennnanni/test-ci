{
  "name": "test-ci",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "description": "",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "express": "^5.2.1"
  },
  "scripts": {
    "prepare": "husky",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check:licenses": "npx license-checker-rseidelsohn --summary --failOn 'GPL-2.0-only;GPL-2.0-or-later;GPL-3.0-only;GPL-3.0-or-later;AGPL-1.0-only;AGPL-3.0-only;AGPL-3.0-or-later;LGPL-2.0-only;LGPL-2.1-only;LGPL-3.0-only;LGPL-3.0-or-later'"
  },
  "lint-staged": {
    "**/*.{ts,tsx,vue,js,cjs,mjs}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "^4.1.4",
    "husky": "^9.1.7",
    "prettier": "^3.8.3",
    "vitest": "^4.1.4"
  }
}
